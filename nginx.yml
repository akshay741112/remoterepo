---
  - name: install nginx     
    hosts:  all 
    become: all
    tasks:
      - name: update apt cache 
        apt:  
          update_cache: yes 
        when: ansible_os_family ==  'debian'

       - name:  install n   
